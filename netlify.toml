# Netlify frontend — see NETLIFY.md
# Redirects are generated into public/_redirects at build time (API proxy + SPA)

[build]
  publish = "public"
  command = "npm run build:netlify"

[build.environment]
  NODE_VERSION = "20"
